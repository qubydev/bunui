# BunUI

Minimal open-source React UI component library based on the HeroUI v3 design language.

## Development

```bash
pnpm install
pnpm dev
```

## Structure

- `src/` — landing page and component showcase
- `packages/react/` — React components
- `packages/styles/` — shared tokens and component styles

Currently included: `Button`.
